[build-system]
requires = ["setuptools>=42", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "x14"
version = "0.1.0"
description = "Multi-TF channel analysis with C++ feature extraction"
requires-python = ">=3.9"
