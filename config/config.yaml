paths:
  data_dir: "data"
  models_dir: "models"
  db_dir: "db"

training:
  symbol: "TSLA"
  benchmark_symbol: "SPY"
  sequence_length: 256
  batch_size: 64
  learning_rate: 0.001
  epochs: 10

backtest:
  test_start: "2023-01-01"
  test_end: "2023-12-31"

alerts:
  confidence_threshold: 0.8

server:
  host: "0.0.0.0"
  port: 8000

